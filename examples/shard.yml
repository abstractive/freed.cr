name: freed-examples
version: 0.0.0
crystal: 0.32.1

authors:
  - digitalextremist <code@extremist.digital>

dependencies:
  coincide:
    git: git@source.freed.network:abstractive/coincide.git
    branch: master
  artillery:
    git: git@source.freed.network:abstractive/artillery.git
    branch: freed
  mongo:
    github: datanoise/mongo.cr
    branch: master

targets:
  artillery:
    main: thinking.cr
