name: freed
version: 0.0.0

description: Persistent decentral data objects.
  
authors:
  - digitalextremist <code@extremist.digital>

crystal: 0.32.1

license: MIT

executables:
  - freed

dependencies:
  coincide:
    git: git@source.freed.network:abstractive/coincide.git
    branch: master
  mongo:
    github: datanoise/mongo.cr
    branch: master

targets:
  freed:
    main: src/processes/mind.cr
